name: WordPress CI

on:
  push:
    branches:
      - main
      - 'feature/**'

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          cd wordpress-app
          docker build -t imfuzail/wordpress-site .
        env:
          DOCKER_BUILDKIT: 1
          COMPOSE_DOCKER_CLI_BUILD: 1

      - name: Push Docker image to Docker Hub
        run: |
          docker push imfuzail/wordpress-site

      - name: Start Docker Compose services
        run: |
          cd wordpress-app
          docker-compose up -d

      # - name: Run tests
      #   run: |
      #     # Wait for services to be healthy
      #     sleep 30
      #     docker-compose -f docker-compose.yml exec wordpress bash -c "curl -sSf http://localhost:8081 > /dev/null"
      #     # Run your tests here, e.g., PHP CodeSniffer and PHPUnit tests
      #     docker-compose -f docker-compose.yml exec wordpress bash -c "vendor/bin/phpcs"
      #     docker-compose -f docker-compose.yml exec wordpress bash -c "vendor/bin/phpunit"

      # - name: Tear down Docker containers
      #   run: |
      #     cd wordpress-app
      #     docker-compose down
